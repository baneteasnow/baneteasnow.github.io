---
#dfdfdf <mark style="background: #FFB8EBA6;">60</mark>
layout: page-3
published: true
last_modified_at: 2026-08-16
title: "2026 yılında okuduğum kitaplar"
goal: 70
read: 45
---

<table id="yearBooksTable">
	<thead>
		<tr>
			<th style="text-align: center">⁜</th>
			<th style="text-align: left">...</th>
			<th style="text-align: left">…</th>
			<th style="text-align: left">pages</th>
			<th style="text-align: left">format</th>
			<th
				id="readingGoalHeader"
				style="text-align: left"
			>
				Loading...
			</th>
		</tr>
	</thead>

    <tbody id="yearBooksBody">
    	<tr>
    		<td colspan="6">Loading...</td>
    	</tr>
    </tbody>

</table>

<p
	id="yearBooksError"
	style="display: none"
>
	Reading list could not be loaded.
</p>
<script>
const YEAR_BOOKS_API_URL =
	"https://script.google.com/macros/s/AKfycbxOR9Us1HPJQy01c5bRUjgCG2zC332Jp4epx66FdHqJK3B-He33E2UOT4saeb_UiqBO/exec?mode=books&year=2026"

    const READING_YEAR = 2026
    const READING_GOAL = Number("{{ page.goal }}") || 70

    function escapeHTML(value) {
    	return String(value ?? "")
    		.replaceAll("&", "&amp;")
    		.replaceAll("<", "&lt;")
    		.replaceAll(">", "&gt;")
    		.replaceAll('"', "&quot;")
    		.replaceAll("'", "&#039;")
    }

    async function loadYearBooks() {
    	const tableBody = document.getElementById("yearBooksBody")
    	const header = document.getElementById("readingGoalHeader")
    	const errorEl = document.getElementById("yearBooksError")

    	try {
    		const response = await fetch(YEAR_BOOKS_API_URL, {
    			cache: "no-store",
    		})

    		if (!response.ok) {
    			throw new Error(`HTTP ${response.status}`)
    		}

    		const data = await response.json()
    		const books = data.books || []

    		const readCount = books.length
    		const percentage =
    			READING_GOAL > 0
    				? Math.round((readCount / READING_GOAL) * 100)
    				: 0

    		header.textContent =
    			`${readCount} / ${READING_GOAL} ${percentage}%`

    		tableBody.innerHTML = books
    			.map((book) => {
    				const month =
    					escapeHTML(book.month) +
    					" " +
    					String(READING_YEAR).slice(-2)

    				const title = book.year
    					? `${escapeHTML(book.title)} (${escapeHTML(book.year)})`
    					: escapeHTML(book.title)

    				return `
    					<tr>
    						<td style="text-align: center">
    							${escapeHTML(book.id)}
    						</td>

    						<td>
    							${title}
    						</td>

    						<td>
    							${escapeHTML(book.author)}
    						</td>

    						<td>
    							${escapeHTML(book.pages)}
    						</td>

    						<td>
    							${escapeHTML(book.format)}
    						</td>

    						<td>
    							${month}
    						</td>
    					</tr>
    				`
    			})
    			.join("")

    		updateReadingProgress(readCount)
    	} catch (error) {
    		console.error(error)

    		tableBody.innerHTML = ""
    		errorEl.style.display = "block"
    	}
    }

    function updateReadingProgress(readCount) {
    	const progress = document.getElementById("readingChallengeProgress")
    	const progressText = document.getElementById("readingChallengeText")

    	if (progress) {
    		progress.value = readCount
    		progress.max = READING_GOAL
    		progress.title = `${readCount}/${READING_GOAL}`
    	}

    	if (progressText) {
    		progressText.textContent = `${readCount}/${READING_GOAL}`
    	}
    }

    loadYearBooks()

</script>
  <br>

<center>
<div style="font-size: 60%; font-style: italic;"> 
  <span class="link1" style="font-style: italic;"><a href="/now" title='şimdi'>şimdi </a></span> &nbsp;
  <span class="link1" style="font-style: italic;"><a href="/2025" title='2025'>2025 </a></span> &nbsp;
  <span class="link1" style="font-style: italic;"><a href="/2024" title='2024'>2024 </a></span> &nbsp;
  <span class="link1" style="font-style: italic;"><a href="/2023" title='2023'>2023 </a></span> &nbsp;
  <span class="link1" style="font-style: italic;"><a href="/2022" title='2022'>2022 </a></span> &nbsp; 
  <span class="link1" style="font-style: italic;"><a href="/2021" title='2021'>2021 </a></span>
</div>
</center>

  <br>

<div
	style="
		display: flex;
		align-items: center;
		gap: 6px;
	"
>
	<progress
		id="readingChallengeProgress"
		value="0"
		max="{{ page.goal }}"
		style="width: 80%;"
	></progress>

    <span
    	id="readingChallengeText"
    	style="
    		font-size: 50%;
    		font-style: italic;
    		white-space: nowrap;
    	"
    >Loading...</span>

</div>

<div style="clear: both"></div>
<br />

<style>
	#yearStatsTable th,
	#yearStatsTable td {
		text-align: center;
		vertical-align: middle;
	}
</style>
<table id="yearStatsTable">
	<thead>
		<tr>
			<th>Jan</th>
			<th>Feb</th>
			<th>Mar</th>
			<th>Apr</th>
			<th>May</th>
			<th>Jun</th>
			<th>Jul</th>
			<th>Aug</th>
			<th>Sep</th>
			<th>Oct</th>
			<th>Nov</th>
			<th>Dec</th>
			<th>total</th>
			<th></th>
		</tr>
	</thead>

    <tbody>
    	<tr id="booksRow">
    		<td colspan="14">Loading...</td>
    	</tr>

    	<tr id="pagesRow"></tr>
    </tbody>

</table>

<p id="yearStatsError" style="display: none;">
	Reading statistics could not be loaded.
</p>
<script>
	const YEAR_STATS_API_URL =
	"https://script.google.com/macros/s/AKfycbxOR9Us1HPJQy01c5bRUjgCG2zC332Jp4epx66FdHqJK3B-He33E2UOT4saeb_UiqBO/exec?year=2026"

    const months = [
    	"Jan",
    	"Feb",
    	"Mar",
    	"Apr",
    	"May",
    	"Jun",
    	"Jul",
    	"Aug",
    	"Sep",
    	"Oct",
    	"Nov",
    	"Dec",
    ]

    async function loadYearStats() {
    	const booksRow = document.getElementById("booksRow")
    	const pagesRow = document.getElementById("pagesRow")
    	const errorEl = document.getElementById("yearStatsError")

    	try {
    		const response = await fetch(YEAR_STATS_API_URL, {
    			cache: "no-store",
    		})

    		if (!response.ok) {
    			throw new Error(`HTTP ${response.status}`)
    		}

    		const data = await response.json()

    		booksRow.innerHTML =
    			months
    				.map((month) => `<td>${data.months[month].books}</td>`)
    				.join("") +
    			`<td>${data.total.books}</td>` +
    			`<td>books</td>`

    		pagesRow.innerHTML =
    			months
    				.map((month) => `<td>${data.months[month].pages}</td>`)
    				.join("") +
    			`<td>${data.total.pages}</td>` +
    			`<td>pages</td>`
    	} catch (error) {
    		console.error(error)

    		booksRow.innerHTML = ""
    		pagesRow.innerHTML = ""
    		errorEl.style.display = "block"
    	}
    }

    loadYearStats()

</script>

<div style="clear: both"></div>
<br />
